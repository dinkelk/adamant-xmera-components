---
#  typedef struct {
#      double data[SUNLINE_FILTER_MAX_CSS * 3]; /*!< [-] one body-frame three-vector per CSS */
#  } SunlineFilterCssMatrix_c;
description: Record wrapping one body frame three-vector per coarse sun sensor slot, three entries per sensor in row major order. Maps to SunlineFilterCssMatrix_c in the sunlineFilter C shim, which takes it by pointer.
fields:
  - name: Value
    description: "[-] Three entries per sensor in row major order."
    type: Packed_F64x24.T
